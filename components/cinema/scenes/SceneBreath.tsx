"use client";

/**
 * Breath scene — pure camera dwell, no DOM. Renders nothing visible;
 * scroll budget contributed by the scene's duration is what creates the
 * breathing room between text-bearing scenes.
 */
export function SceneBreath() {
  return null;
}
